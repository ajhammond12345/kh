{
  "name": "kh-website",
  "version": "1.0.0",
  "private": true,
  "description": "The Knighton-Hammond Charitable Trust Website",
  "workspaces": [
    "packages/*"
  ],
  "scripts": {
    "dev": "concurrently \"npm run dev -w packages/backend\" \"npm run dev -w packages/frontend\"",
    "build": "npm run build -w packages/frontend && npm run build -w packages/backend",
    "start": "npm run start -w packages/backend"
  },
  "devDependencies": {
    "better-sqlite3": "^13.0.3",
    "concurrently": "^9.1.0",
    "firebase-admin": "^14.2.0",
    "tsx": "^4.23.12",
    "typescript": "^5.7.0"
  }
}
